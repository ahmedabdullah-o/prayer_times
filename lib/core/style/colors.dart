import 'dart:ui';

class Colors {
  Colors._(); // no instantiation

  // Branding
  static const primary = Color(0xFF7CCCDE);
  static const primary50 = Color(0x887CCCDE);

  // Neutral palette
  static const foreground = Color(0xFFFAEEBE);
  static const background = Color(0xFFFFF7D6);
  static const surface = Color(0xFFFFFAE5);
  static const text = Color(0xFF262520);
  static const textSecondary = Color(0xFF4D4A40);

  // Alerts
  static const success = Color(0xFF4CAF50);
  static const error = Color(0xFFE53935);
  static const warning = Color(0xFFFFB300);
}
